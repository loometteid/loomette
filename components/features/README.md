# Feature components

Feature-specific components, grouped by domain (e.g.
`components/features/wardrobe/`, `components/features/outfit-builder/`).

Only `components/ui/` holds generic shadcn primitives. Create a
subfolder here once a feature has real scope from Figma — don't
pre-create empty domain folders speculatively.
