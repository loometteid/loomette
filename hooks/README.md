# Hooks

Custom React hooks shared across components.

Add one here only when the logic is reused across two or more
components — otherwise keep it local to the component. Three similar
lines beat a premature abstraction.
